import 'package:flutter/material.dart';

class CustomObj extends StatelessWidget {
  const CustomObj({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hello"),
      ),
    );
  }
}
